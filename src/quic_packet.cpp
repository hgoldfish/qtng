// Packet header helpers live alongside frames; this TU keeps the CMake module split.
#include "qtng/private/quic_p.h"

namespace qtng {

// Intentionally empty: quicBuild*/quicParse* are in quic_frame.cpp.

}  // namespace qtng
